let param_1 = 0;
let param_2 = 0;
let symbol;

function my_f (param_1, symbol, param_2) {
	console.log(param);
		if ( param ===  '+' param ===  '-' param ===  '*' param ===  '/') {

		if (param == '+') {
			return (true);
		}

		else if (param== '-') {
			return (true);
		}

		else if (param == '*') {
			return(true);
		} 

		else if (param == '/') {
			return(true);
		}

		else (param === param_1){
			return (false);
		}
	}

}
my_f ();


